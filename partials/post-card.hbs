<article class="gh-card {{post_class}}">
  <a class="gh-card-link" href="{{url}}">
    {{#if feature_image}}
      <div class="gh-card-image">
        <img
          srcset="
            {{img_url feature_image size="s"}}  800w,
            {{img_url feature_image size="m"}} 1200w
          "
          sizes="(min-width: 1000px) 400px, 100vw"
          src="{{img_url feature_image size="s"}}"
          alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
          loading="lazy"
        >
        {{#if primary_tag}}
          <span class="gh-card-image-tag">{{primary_tag.name}}</span>
        {{/if}}
      </div>
    {{/if}}
    <div class="gh-card-content">
      {{#if primary_tag}}
        <div class="gh-card-category">{{primary_tag.name}}</div>
      {{/if}}
      <h2 class="gh-card-title">{{title}}</h2>
      {{#if excerpt}}
        <p class="gh-card-excerpt">{{excerpt words="20"}}</p>
      {{/if}}
      <div class="gh-card-meta">
        <span>{{date format="MMM D, YYYY"}}</span>
        <span class="gh-card-meta-dot"></span>
        <span>{{reading_time}}</span>
        {{#if featured}}
          <span class="gh-card-featured-badge">Featured</span>
        {{/if}}
      </div>
    </div>
  </a>
</article>
